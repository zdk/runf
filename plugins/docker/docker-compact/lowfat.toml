[plugin]
name = "docker-compact"
version = "0.5.5"
description = "Compact docker ps/images/logs/build/pull/compose output for LLM contexts"
commands = ["docker"]
subcommands = ["ps", "images", "logs", "build", "pull", "compose"]
